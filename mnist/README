Needed setup:

1. Grab the four files from http://yann.lecun.com/exdb/mnist/
2. Decompress them into data/

The code uses python3, numpy, matplotlib

We're generally following http://neuralnetworksanddeeplearning.com/
and its example code, at least to start.
